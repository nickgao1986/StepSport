package com.pic.optimize.rotatemenu;

public class MenuItemInfo extends RCTabItem {
    public MenuItemInfo(String tabName, int tabId, int tabIcon, int tabTitle) {
        super(tabName, tabId, tabIcon, tabTitle);
    }
}
